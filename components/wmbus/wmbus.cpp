#include "wmbus.h"

namespace esphome {
namespace wmbus {

// This component is intentionally empty.
// It exists only for backward compatibility to prevent
// "component not found" errors when users explicitly list
// "wmbus" in their external_components configuration.

}  // namespace wmbus
}  // namespace esphome
